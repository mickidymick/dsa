#!/usr/bin/env bash

# sudo chown -R root:DSA /dev/dsa
# sudo chmod -R g+rw /dev/dsa
# sudo chmod -R a+r+w+x /dev/dsa

sudo chown -R root:DSA /dev/dsa/wq*
sudo chown -R root:DSA /sys/bus/dsa/devices/*
sudo chmod -R a+r+w+x /dev/dsa/wq*
sudo chmod -R a+r+w+x /sys/bus/dsa/devices/*

# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa0/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa0/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa1/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa1/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa2/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa2/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa3/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa3/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa4/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa4/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa5/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa5/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa6/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa6/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa7/*
# sudo chown -R root:DSA /sys/bus/dsa/devices/dsa7/*

# sudo chown -R root:DSA /dev/dsa/wq0.0
# sudo chown -R root:DSA /dev/dsa/wq0.1
# sudo chown -R root:DSA /dev/dsa/wq0.2
# sudo chown -R root:DSA /dev/dsa/wq0.3

# sudo chown -R root:DSA /dev/dsa/wq1.0
# sudo chown -R root:DSA /dev/dsa/wq1.1
# sudo chown -R root:DSA /dev/dsa/wq1.2
# sudo chown -R root:DSA /dev/dsa/wq1.3

# sudo chown -R root:DSA /dev/dsa/wq2.0
# sudo chown -R root:DSA /dev/dsa/wq2.1
# sudo chown -R root:DSA /dev/dsa/wq2.2
# sudo chown -R root:DSA /dev/dsa/wq2.3

# sudo chown -R root:DSA /dev/dsa/wq3.0
# sudo chown -R root:DSA /dev/dsa/wq3.1
# sudo chown -R root:DSA /dev/dsa/wq3.2
# sudo chown -R root:DSA /dev/dsa/wq3.3

# sudo chown -R root:DSA /dev/dsa/wq4.0
# sudo chown -R root:DSA /dev/dsa/wq4.1
# sudo chown -R root:DSA /dev/dsa/wq4.2
# sudo chown -R root:DSA /dev/dsa/wq4.3

# sudo chown -R root:DSA /dev/dsa/wq5.0
# sudo chown -R root:DSA /dev/dsa/wq5.1
# sudo chown -R root:DSA /dev/dsa/wq5.2
# sudo chown -R root:DSA /dev/dsa/wq5.3

# sudo chown -R root:DSA /dev/dsa/wq6.0
# sudo chown -R root:DSA /dev/dsa/wq6.1
# sudo chown -R root:DSA /dev/dsa/wq6.2
# sudo chown -R root:DSA /dev/dsa/wq6.3

# sudo chown -R root:DSA /dev/dsa/wq7.0
# sudo chown -R root:DSA /dev/dsa/wq7.1
# sudo chown -R root:DSA /dev/dsa/wq7.2
# sudo chown -R root:DSA /dev/dsa/wq7.3

# sudo chmod -R g+r+w /dev/dsa/wq0.0
# sudo chmod -R g+r+w /dev/dsa/wq0.1
# sudo chmod -R g+r+w /dev/dsa/wq0.2
# sudo chmod -R g+r+w /dev/dsa/wq0.3

# sudo chmod -R g+r+w /dev/dsa/wq1.0
# sudo chmod -R g+r+w /dev/dsa/wq1.1
# sudo chmod -R g+r+w /dev/dsa/wq1.2
# sudo chmod -R g+r+w /dev/dsa/wq1.3

# sudo chmod -R g+r+w /dev/dsa/wq2.0
# sudo chmod -R g+r+w /dev/dsa/wq2.1
# sudo chmod -R g+r+w /dev/dsa/wq2.2
# sudo chmod -R g+r+w /dev/dsa/wq2.3

# sudo chmod -R g+r+w /dev/dsa/wq3.0
# sudo chmod -R g+r+w /dev/dsa/wq3.1
# sudo chmod -R g+r+w /dev/dsa/wq3.2
# sudo chmod -R g+r+w /dev/dsa/wq3.3

# sudo chmod -R g+r+w /dev/dsa/wq4.0
# sudo chmod -R g+r+w /dev/dsa/wq4.1
# sudo chmod -R g+r+w /dev/dsa/wq4.2
# sudo chmod -R g+r+w /dev/dsa/wq4.3

# sudo chmod -R g+r+w /dev/dsa/wq5.0
# sudo chmod -R g+r+w /dev/dsa/wq5.1
# sudo chmod -R g+r+w /dev/dsa/wq5.2
# sudo chmod -R g+r+w /dev/dsa/wq5.3

# sudo chmod -R g+r+w /dev/dsa/wq6.0
# sudo chmod -R g+r+w /dev/dsa/wq6.1
# sudo chmod -R g+r+w /dev/dsa/wq6.2
# sudo chmod -R g+r+w /dev/dsa/wq6.3

# sudo chmod -R g+r+w /dev/dsa/wq7.0
# sudo chmod -R g+r+w /dev/dsa/wq7.1
# sudo chmod -R g+r+w /dev/dsa/wq7.2
# sudo chmod -R g+r+w /dev/dsa/wq7.3
